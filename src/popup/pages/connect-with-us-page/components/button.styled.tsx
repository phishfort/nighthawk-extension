import styled from '@emotion/styled';

export const StyledButton = styled.div`
	display: flex;
	align-items: center;
	cursor: pointer;
	gap: 4px;

	button {
		background: none;
		outline: none;
		border: 0;
		color: white;
		text-align: left;
		letter-spacing: 0px;
		opacity: 1;
	}
`;
